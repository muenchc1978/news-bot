name: Auto-Commit Sources

on:
  push:  # Startet bei jedem Push
  workflow_dispatch:  # Manuelles Starten möglich
  schedule:
    - cron: '0 * * * *'  # Läuft jede Stunde (anpassbar)

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - name: Repository klonen
        uses: actions/checkout@v4

      - name: Git konfigurieren
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub AutoBot"

      - name: Änderungen committen und pushen
        run: |
          git add sources.json
          git commit -m "🔄 Automatische Aktualisierung der Quellen" || echo "ℹ️ Keine Änderungen vorhanden"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
